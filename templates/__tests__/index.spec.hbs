import { mount } from '@vue/test-utils'
import Oa{{hump name}} from '@dongdong-components/{{name}}'

describe('@dongdong-components/{{name}}', () => {
  it('create', () => {
    const wrapper = mount(Oa{{hump name}})
    expect(wrapper.classes()).toContain('oa-{{name}}')
  })
})
